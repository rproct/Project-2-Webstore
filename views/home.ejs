<!DOCTYPE html>
<html>
    <head>
        <%- include('templates/head')%>
    </head>
    <body class="bg-light">
        <%- include('templates/topbar')%>

        <div class="container d-flex flex-wrap">
            <% for (var i = 0; i < grasses.length; i++) { %>
                <%- include('templates/product_small', {grass : grasses[i]})%>
            <% } %>
        </div>
    </body>
</html>